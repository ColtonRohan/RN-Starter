import React from "react";
import { View, Text, StyleSheet, FlatList } from "react-native";

const ImageCompoenent = (props) => {
	return <Text>{props.title}</Text>;
};

export default ImageCompoenent;
